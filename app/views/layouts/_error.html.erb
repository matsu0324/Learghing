<% if obj.errors.any? %>
  <div id="error_explanation">
    <h3 class="alert"><%= pluralize(obj.errors.count, "個のエラーが発生しました。") %></h3>
    <ul>
      <% obj.errors.full_messages.each do |message| %>
        <li class="alert"><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
