<% if post.errors.any? %>
  <div id="error_explanation">
    <h3><%= pluralize(post.errors.count, "error") %> prohibited this object from being saved:</h3>
	<ul>
	  <% post.errors.full_messages.each do |message| %>
	    <li><%= message %></li>
	  <% end %>
	 </ul>
  </div>
<% end %>